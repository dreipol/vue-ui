/**
 * Here we can define all the user options that can affect the rendering of the input fields
 */
export default {
  props: {
    hasFloatingLabel: {
      type: Boolean,
      default: false,
    },
  },
}
